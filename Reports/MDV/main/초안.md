# 일일 작업 레포트 - 2026년 04월 13일

## 📋 프로젝트 정보
- **전체 프로젝트**: MDV — C++ 기반 마크다운 뷰어+편집기 (Obsidian 유사)
- **현재 단계**: Phase 0 시작 전 — 계획 수립 완료
- **목표**: 프로젝트 성공(완성) + C++/DirectX11/ImGui 렌더링 지식 증진
- **작업 브랜치**: main

---

## ✅ 완료된 작업 (기획)

**생성된 파일**:
- `C:\Users\Dove\.claude\plans\vectorized-wobbling-pelican.md` — 전체 구현 계획 문서

**오늘 수행한 작업**:
- Gemini 제안서 검토 및 방향 결정
- 기술 스택 확정 (ImGui + MD4C + DirectX11)
- A안(Tauri) vs C안(ImGui+DX11) 장단점 비교 분석
- Phase 0~6 전체 구현 로드맵 수립
- 프로젝트 디렉토리 구조 설계
- 핵심 클래스 인터페이스 설계 (DX11Context, Document, MarkdownParser 등)

---

## 📊 전체 로드맵 현황 (0/7 단계 완료)

1. ⏳ **Phase 0 — 환경 구축** (다음 작업)
   - [ ] vcpkg 설치 및 FreeType x64-windows 설치
   - [ ] git submodule 추가 (ImGui docking, MD4C)
   - [ ] CMakeLists.txt 작성
   - [ ] CMakePresets.json 작성
2. ⏳ **Phase 1 — DX11 + ImGui 기본 루프**
   - [ ] `Window.h/.cpp` 작성 — Win32 WNDCLASSEX, 창 생성, 메시지 루프
   - [ ] `DX11Context.h/.cpp` 작성 — Device/SwapChain/RTV 생성, Present, Resize
   - [ ] `ImGuiRenderer.h/.cpp` 작성 — ImGui_ImplWin32_Init, ImGui_ImplDX11_Init
   - [ ] `Application.h/.cpp` 작성 — Run() 루프 (BeginFrame → ImGui → EndFrame)
   - [ ] `Timer.h/.cpp` 작성 — std::chrono 기반 delta time 계산 (Run() 루프에서 사용)
   - [ ] `main.cpp` 작성 — Application 초기화 및 진입점
   - [ ] 검증: ImGui Demo 창 렌더링, 창 리사이즈 정상 동작
3. ⏳ **Phase 2 — Split View + 파일 I/O**
   - [ ] `Document.h/.cpp` 작성 — 텍스트 버퍼 소유, Load/Save (std::fstream)
   - [ ] `EditorPane.h/.cpp` 작성 — InputTextMultiline + std::string 동적 버퍼 연동
   - [ ] `MainLayout.h/.cpp` 작성 — ImGui Child Window 좌/우 2분할
   - [ ] 우측 PreviewPane Child Window 작성 — 편집 텍스트를 그대로 출력 (Phase 3 렌더러 연결 전 임시)
   - [ ] `MenuBar.h/.cpp` 작성 — 파일 열기(GetOpenFileName), 저장(Ctrl+S)
   - [ ] 검증: 파일 열기 → 편집 → Ctrl+S → 재열기 정상 복원
4. ⏳ **Phase 3 — MD4C 파서 + 마크다운 렌더러**
   - [ ] `MarkdownParser.h/.cpp` 작성 — MD4C SAX 콜백 → MdNode 벡터 변환
   - [ ] `MarkdownRenderer.h/.cpp` 작성 — MdNode 순회 → ImGui API 호출
   - [ ] Heading 렌더링 (`#`) — PushFont + 레벨별 크기 조절
   - [ ] Bold / Italic 렌더링 — 폰트 스택 교체
   - [ ] HRule 렌더링 — ImGui::Separator()
   - [ ] 인라인 코드 렌더링 — TextColored + 배경색
   - [ ] 코드 블록 렌더링 — Child Window + 배경색
   - [ ] 리스트 렌더링 — 들여쓰기 + bullet 문자
   - [ ] BlockQuote 렌더링 — 왼쪽 컬러 바 + 들여쓰기
   - [ ] 링크 렌더링 — 색상 + 클릭 시 ShellExecuteA
   - [ ] 검증: README.md 열면 각 요소가 시각적으로 구분됨
5. ⏳ **Phase 4 — 폰트 & 테마 설정**
   - [ ] `Theme.h/.cpp` 작성 — 색상 팔레트 구조체, ImGuiStyle 일괄 적용
   - [ ] `FontManager.h/.cpp` 작성 — FreeType 폰트 로드, ImFontAtlas 재빌드
   - [ ] `SettingsWindow.h/.cpp` 작성 — ColorEdit4(글자/배경/헤딩색), SliderFloat(폰트 크기)
   - [ ] nlohmann/json 추가 (FetchContent) — settings.json 읽기/쓰기
   - [ ] 한국어 폰트 적용 — GetGlyphRangesKorean() + NanumGothic/Noto Sans KR
   - [ ] 검증: 설정 변경 즉시 반영, 앱 재시작 후 설정 복원
6. ⏳ **Phase 5 — 자동저장 & 안정화**
   - [ ] `Document::Tick()` 구현 — delta time 누적 → 인터벌 초과 시 Save()
   - [ ] 창 타이틀 dirty 표시 — 미저장 시 파일명 뒤 `*` 접미사
   - [ ] WM_CLOSE 처리 — IsDirty() 확인 → MessageBox 저장 여부 경고
   - [ ] 자동저장 간격 설정 UI — SettingsWindow **수정** (슬라이더 추가, 10~300초)
   - [ ] 검증: 편집 후 30초 방치 → 자동 저장 확인, 닫기 경고 동작
7. ⏳ **Phase 6 — 선택 기능** (이미지, 파일 탐색기, 검색/치환, 탭, 구문 강조)
   - [ ] 파일 탐색기 패널 — std::filesystem 디렉토리 열거, 좌측 패널 추가
   - [ ] 검색/치환 — InputText + std::string::find/replace, 단축키 Ctrl+F
   - [ ] 이미지 인라인 표시 — DX11 텍스처 로드 → ImGui::Image()
   - [ ] 문서 탭 — ImGui::TabBar, 다중 Document 인스턴스 관리
   - [ ] 구문 강조 (편집기) — DrawList로 편집 영역 텍스트 색칠

---

## 💡 학습 내용

- **ImGui Docking Branch**: Split View 레이아웃 구현에 DockSpace API 활용 가능
- **MD4C SAX 파서 패턴**: 콜백 방식으로 마크다운 파싱 → ParseState 스택으로 노드 트리 구성
- **DX11 SwapChain 리사이즈 패턴**: WM_SIZE에서 RTV 해제 후 재생성 필수
- **ImGui InputText 동적 버퍼**: CallbackResize 플래그로 std::string 연동 가능
- **FreeType 아틀라스 재빌드**: InvalidateDeviceObjects → Rebuild → CreateDeviceObjects 순서
- **언리얼-DX11 연결점**: SwapChain+RTV ↔ FRHIViewport+BackBuffer 개념 대응

---

**작성일**: 2026년 04월 13일
